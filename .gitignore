# PRIVATE AUDIO — your personal music files. NEVER commit/deploy these
# (copyrighted; the public repo + Vercel would redistribute them). The app reads
# this folder only in local dev; production play uses in-app import (client-side).
private_audio/
private-audio/

# dependencies
node_modules/

# build output
dist/
.vite/

# test artifacts (generated screenshots, etc.)
tests/e2e/*.png

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.local

# env
.env
.env.*
!.env.example
